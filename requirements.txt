# --- Core (Phase 0) ---
anthropic>=0.40.0        # Messages API + web_search tool (fact-check judge)
python-docx>=1.1.0       # .docx export
openpyxl>=3.1.0          # .xlsx export

# --- Local STT (needed for --wav and for live streaming) ---
faster-whisper>=1.0.0    # local Whisper; set whisper_device="cuda" to use your GPU
numpy>=1.24              # audio buffers

# --- Live desktop app (run_gui.py / GroundTruth.exe) ---
fastapi>=0.110           # backend + WebSocket event stream
uvicorn[standard]>=0.29  # ASGI server
soundcard>=0.4.3         # mic + WASAPI system-loopback capture (Windows)

# Optional, recommended for source checkouts: upgrades the built-in energy-based
# voice gate to webrtcvad (sharper speech/silence boundaries). The packaged .exe
# ships without it and uses the energy gate.
# webrtcvad-wheels>=2.0.14

# --- GPU acceleration (optional; NVIDIA only) ---
# Provides the CUDA runtime DLLs faster-whisper/ctranslate2 need to use the GPU.
# With these installed and Whisper device = auto/cuda, transcription runs on the
# GPU (much faster); without them it falls back to CPU automatically.
# nvidia-cublas-cu12
# nvidia-cuda-runtime-cu12
# nvidia-cudnn-cu12

# --- Packaging (optional) ---
# pyinstaller>=6.0        # build the standalone .exe from build/GroundTruth.spec
